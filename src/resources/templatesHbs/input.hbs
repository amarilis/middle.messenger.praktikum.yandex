<div class="input" data-input-name="{{name}}">
  <input id="{{name}}" name="{{name}}" type="{{type}}" class="input__field" autocomplete="off" placeholder="  " />
  <label for="{{name}}" class="input__placeholder">{{label}}</label>
  {{eye}} {{errorLine}}
</div>
